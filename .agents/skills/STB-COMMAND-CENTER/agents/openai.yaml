interface:
  display_name: "STB COMMAND CENTER"
  short_description: "Repo-specific patterns and workflows for STB-COMMAND-CENTER"
  default_prompt: "Use the STB-COMMAND-CENTER repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true